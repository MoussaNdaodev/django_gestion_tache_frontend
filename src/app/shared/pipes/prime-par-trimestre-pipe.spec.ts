import { PrimeParTrimestrePipe } from './prime-par-trimestre-pipe';

describe('PrimeParTrimestrePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeParTrimestrePipe();
    expect(pipe).toBeTruthy();
  });
});
